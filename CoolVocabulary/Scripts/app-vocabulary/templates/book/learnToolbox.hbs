<div class="col-xs-4">
	{{#link-to 'book' id="currentBook"}}
	{{model.name}}
	{{/link-to}}
</div>
<div class="col-xs-8 text-right">
	<nav class="words-nav flex-centered">
		{{#each sessionBookWords as |bookWord|}}
			<span class="word-circle" title="{{bookWord.word.value}}"></span>
		{{/each}}
	</nav>
</div>