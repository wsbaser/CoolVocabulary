<span class="month-info-wrap">
	<span class="month">
		{{month}}
	</span>
	{{#if hasStar}}
		<i class="star-icon"></i>
	{{/if}}
</span>
<span class="days" >
	{{#each days as |day|}}
		<span class="day {{if day.taskFailed "failed"}}">
			{{input type="checkbox" disabled="true" checked=day.taskCompleted class="checkbox-custom"}}
			<label class="checkbox-custom-label"></label>
		</span>
	{{/each}}
</span>